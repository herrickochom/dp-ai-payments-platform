FROM python:3.11-slim

# Copy requirements and install
COPY services/agents/modeling/requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt

WORKDIR /app/src

ENTRYPOINT ["python3", "-m", "main.py"]
