FROM python:3.11-slim

# Copy requirements and install
COPY services/agents/dq/requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt

WORKDIR /app/src

ENV LOG_DIR=/logs

ENTRYPOINT ["python3", "-m", "agent.py"]
