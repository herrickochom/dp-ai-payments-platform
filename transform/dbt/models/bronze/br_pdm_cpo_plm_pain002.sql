{{ config(
    materialized='iceberg_table',
    tags=['bronze', 'cpo', 'plm', 'technical']
) }}
select
    event_id,
    envelope_message_id,
    event_family,
    message_id,
    event_type,
    correlation_id,
    instruction_id,
    end_to_end_id,
    transaction_id,
    uetr,
    business_reference,
    x_trace,
    x_channel,
    x_source_system,
    x_target_system,
    x_service,
    x_operation,
    x_component,
    x_node,
    x_host,
    x_provider,
    x_network,
    x_beneficiary_sa,
    x_wallet_reference,
    x_provider_transaction_id,
    x_credit_status,
    x_agent_reference,
    x_cashout_status,
    component,
    technical_stage,
    technical_status,
    event_timestamp,
    processing_timestamp,
    latency_ms,
    error_code,
    error_category,
    retry_count,
    timeout_indicator,
    kafka_topic,
    kafka_partition,
    kafka_offset,
    kafka_timestamp,
    category,
    source_system,
    year,
    month,
    day,
    load_timestamp,
    record_source
from {{ ref('stg_pdm_cpo_plm_pain002') }}