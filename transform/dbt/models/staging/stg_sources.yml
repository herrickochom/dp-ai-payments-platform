version: 2

# Inventory of the persisted Bronze relations available to staging. Staging
# models use ref() because these relations are built by this dbt project.
sources:
  - name: bronze_inventory
    description: Persisted Bronze payment relations managed by this dbt project.
    database: lakehouse
    schema: bronze
    tables:
      - name: br_icmn_vpm_pain001
      - name: br_icmn_pmn_pain001
      - name: br_cpo_psn_pain002
      - name: br_cpo_plm_pain002
